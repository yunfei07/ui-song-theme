/**
 * Song-inspired theme plugin host entry. The browser entry contributes the
 * visual token layer declared by the package's `dsh.client` metadata.
 */
/** Host plugin body with no host-side behavior. */
export declare function apply(): void;
